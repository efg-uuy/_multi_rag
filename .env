# .env文件配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
AUDIT_DB_NAME=audit_logs
# Milvus配置（核心）
MILVUS_HOST=localhost
MILVUS_PORT=19530
MILVUS_COLLECTION=rag_finance_chunks

# 嵌入模型配置（需与Milvus维度一致）
LOCAL_EMBEDDING_MODEL=text-embedding-3-small
MILVUS_DIM=1536  # text-embedding-3-small输出维度

# 其他原有配置
REDIS_HOST=localhost
REDIS_PORT=6379
LOCAL_API_KEY=your_api_key
LOCAL_BASE_URL=your_base_url
LOCAL_TEXT_MODEL=qwen2.5-7b-instruct
# .env 文件新增
EMBEDDING_MODEL_VERSION=bge-m3-v202405  # 与向量库中存储的版本对应
# 本地LLM配置（A6000 GPU）
LOCAL_LLM_API_KEY=your_local_api_key
LOCAL_LLM_BASE_URL=http://localhost:8000/v1  # 本地模型服务地址（如vllm部署）
LOCAL_LLM_MODEL=qwen2.5-7b-instruct  # 本地轻量模型

# 云端LLM配置
CLOUD_LLM_API_KEY=your_cloud_api_key
CLOUD_LLM_BASE_URL=https://api.cloud-llm.com/v1  # 云端服务地址
CLOUD_LLM_MODEL=qwen2.5-72b-instruct  # 云端完整版模型

# 其他原有配置
MILVUS_HOST=localhost
MILVUS_PORT=19530
REDIS_HOST=localhost
EMBEDDING_MODEL=text-embedding-3-small
# .env 新增配置
ENABLE_FINANCE_MODE=True  # 启用金融增强解析（默认False）
FINANCE_TABLE_FIX=True    # 启用跨页表格修复
ENABLE_FINANCE_MODE=True
LOCAL_EMBEDDING_MODEL=BAAI/bge-m3-finance  # 金融向量模型
